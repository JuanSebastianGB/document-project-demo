/**
 * User
 * @typedef {object} User
 * @property {number} id User's id
 * @property {string} name User's name
 * @property {string} [lastName] User's last name (Optional)
 * @property {number | string} age User's age
 * @property {boolean} isActive User's status
 */


/**
 * jsdoc to call a custom prototype
 * @type {User}
 */
const newUser = {
    id: 1,
    name: 'user Name',
    lastName: 'user Last Name',
    age: 32,
    isActive: true
}