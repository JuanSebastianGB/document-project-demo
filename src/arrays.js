/**
 * jsdocs test for lists
 * @type {Array}
 */
const listOfAny = [1, 3, 50, 23, "5", {}, []];

/**
 * jsdocs test for Numbers
 * @type {number}
 */
const oneNumber = 55;

/**
 * jsdocs test for list of numbers
 * @type {Array<number>}
 */
const listOfNumbers = [5, 5.5, 5, 3];

/**
 * jsdocs test for mixed list that includes numbers and strings
 * @type {Array<Number|string>}
 */
const arrayOfNumbersAndStrings = ['1', 2];